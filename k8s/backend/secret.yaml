# ============================================================
# Secret — Backend Spring Boot (DEV) — TEMPLATE
#
# Ce fichier est un TEMPLATE. Ne jamais commiter de vraies
# valeurs sensibles dans le dépôt Git.
#
# Instructions de création manuelle:
#   kubectl create secret generic backend-secret \
#     --from-literal=DB_PASSWORD="<your-db-password>" \
#     --from-literal=JWT_SECRET="<your-jwt-secret>" \
#     --namespace=village-dev
#
# Ou avec un fichier .env (NON commité) :
#   kubectl create secret generic backend-secret \
#     --from-env-file=.env.dev \
#     --namespace=village-dev
#
# En production, utiliser :
#   - External Secrets Operator (https://external-secrets.io)
#   - HashiCorp Vault (https://www.vaultproject.io)
#   - Sealed Secrets (https://sealed-secrets.netlify.app)
# ============================================================
#
# IMPORTANT: Ce fichier ne doit PAS être appliqué directement.
#            Créer le secret manuellement via la commande ci-dessus.
#
# Pour référence uniquement — structure du Secret :
#
# apiVersion: v1
# kind: Secret
# metadata:
#   name: backend-secret
#   namespace: village-dev
# type: Opaque
# stringData:
#   DB_PASSWORD: "<replace-with-actual-password>"
#   JWT_SECRET: "<replace-with-actual-secret-key>"
